namespace Fogos.Importer.Tests;

/// <summary>Placeholder — the golden per-collection mapper tests land in Phase 1.</summary>
public class PlaceholderTests
{
    [Fact]
    public void Importer_project_is_wired()
    {
        Assert.True(true);
    }
}
